<div class="col-md-8">
  <h3><%= @genres.name %>一覧</h3>
　　　<div>
        <% @items.each do |item| %>
          <%= link_to item_path(item.id) do %>
            <%= image_tag item.image, size: "200x150" %>
          <% end %>
          <p><%= item.name %></p>
          <p>¥<%= item.price %></p>
        <% end %>
      </div>
  </div>